<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.chm.mapper.ClassMapper">
    <resultMap id="BaseResultMap" type="com.chm.domain.Class">
        <id column="ClassID" property="classid" jdbcType="VARCHAR"/>
        <result column="professional" property="professional" jdbcType="VARCHAR"/>
        <result column="count" property="count" jdbcType="INTEGER"/>
    </resultMap>
    <sql id="Base_Column_List">
    ClassID, professional, count
  </sql>
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.String">
        select
        <include refid="Base_Column_List"/>
        from tbl_class
        where ClassID = #{classid,jdbcType=VARCHAR}
    </select>
    <select id="getLabels" resultType="java.util.List">
      select distinct ClassId
      from tbl_student
      where StuID in(
        select StuID
        from tbl_select_course
        where CouID in(
          select CouID
          from tbl_teach
          where TeachID = #{teachid,jdbcType=INTEGER}
        )
      )
  </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.String">
    delete from tbl_class
    where ClassID = #{classid,jdbcType=VARCHAR}
  </delete>
    <insert id="insert" parameterType="com.chm.domain.Class">
    insert into tbl_class (ClassID, professional, count
      )
    values (#{classid,jdbcType=VARCHAR}, #{professional,jdbcType=VARCHAR}, #{count,jdbcType=INTEGER}
      )
  </insert>
    <insert id="insertSelective" parameterType="com.chm.domain.Class">
        insert into tbl_class
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="classid != null">
                ClassID,
            </if>
            <if test="professional != null">
                professional,
            </if>
            <if test="count != null">
                count,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="classid != null">
                #{classid,jdbcType=VARCHAR},
            </if>
            <if test="professional != null">
                #{professional,jdbcType=VARCHAR},
            </if>
            <if test="count != null">
                #{count,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.chm.domain.Class">
        update tbl_class
        <set>
            <if test="professional != null">
                professional = #{professional,jdbcType=VARCHAR},
            </if>
            <if test="count != null">
                count = #{count,jdbcType=INTEGER},
            </if>
        </set>
        where ClassID = #{classid,jdbcType=VARCHAR}
    </update>
    <update id="updateByPrimaryKey" parameterType="com.chm.domain.Class">
    update tbl_class
    set professional = #{professional,jdbcType=VARCHAR},
      count = #{count,jdbcType=INTEGER}
    where ClassID = #{classid,jdbcType=VARCHAR}
  </update>
</mapper>