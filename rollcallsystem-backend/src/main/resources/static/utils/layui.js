var layer, element, form, laypage, table

layui.use(['layer', 'element', 'form', 'laypage', 'table'], function () {
    layer = layui.layer
    element = layui.element
    form = layui.form
    laypage = layui.laypage
    table = layui.table
});